# PlatformIO
.pio/
.vscode/
*.bin
!bin/*.bin

# build / OS junk
*.elf
*.map
.DS_Store
Thumbs.db

# secrets
secrets.h
*.local.h

# AI harness local notes — machine-local, never ships public
CLAUDE.local.md
dist/
.qwen/

# desktop harness — vendored dep (fetched by host/build.sh) + build output
host/vendor/miniaudio.h
host/desktop_synth
host/desktop_synth.exe
host/*.wav
